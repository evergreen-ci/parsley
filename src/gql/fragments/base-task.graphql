fragment BaseTask on Task {
  displayName
  execution
  id
  logs {
    agentLogLink
    allLogLink
    systemLogLink
    taskLogLink
  }
  patchNumber
  status
  versionMetadata {
    id
    isPatch
    message
    projectIdentifier
    revision
  }
}
