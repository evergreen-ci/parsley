fragment BaseTask on Task {
  displayName
  execution
  id
  patchNumber
  status
  tests(opts: null) {
    testResults {
      id
      logs {
        urlRaw
      }
      status
      testFile
    }
  }
  versionMetadata {
    id
    isPatch
    message
    projectIdentifier
    revision
  }
}
